import { getLinesFromText } from "../utils.js";

const first = (data) => {
    const lines = getLinesFromText(data);

    //TODO
}

const second = (data) => {
    const lines = getLinesFromText(data);

    //TODO
}

export {
    first as templateFirst,
    second as templateSecond
}